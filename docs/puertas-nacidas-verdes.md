# Puertas nacidas verdes, y su fecha de revisión

> **Para qué es este fichero.** Una puerta que nace verde sobre el árbol real no ha demostrado que vigile: ha demostrado que hoy no hay nada que cazar. Puede ser que vigile poco, puede ser que llegara tarde, y las dos cosas hay que saberlas. Así que se anota, con fecha, y se revisa **a los quince días**.
>
> **La fila la escribe quien escribe la puerta, no quien la revisa.** Si la escribiera el revisor, la lista sólo tendría las puertas que alguien se acordó de mirar, que son justo las que no hacen falta en una lista.
>
> **Qué NO entra aquí.** Una puerta que nace **roja** sobre el árbol y se pone verde al arreglar lo que encontró. Ésa ya ha vigilado algo que nadie le puso delante, que es exactamente lo que este fichero busca comprobar. Se anota en su commit y se acabó.

## La regla de revisión, con sus dos salidas

A los quince días se mira **si la puerta ha estado roja alguna vez sin que su autor la rompiera a propósito**. Se contesta con el historial (`git log`, la salida de CI), no de memoria.

- **Ha cazado algo**: se anota qué, se saca la fila de esta tabla y la puerta pasa a ser una puerta normal.
- **No ha cazado nada**: se decide **explícitamente** entre dos cosas, y no se deja pasar otra quincena. O **vigila poco**, y entonces o se amplía o se dice que cubre menos de lo que su nombre promete. O **llegó tarde**, y entonces está bien que exista y lo que hay que preguntarse es qué otra puerta habría hecho falta ANTES.

## Abiertas

| Puerta | Nació | Se revisa | Por qué nació verde | Qué se sabe hoy |
|---|---|---|---|---|
| `TestGoSumNoEsUnRecipienteVacio` | 04-09-2026 | **19-09-2026** | nació en el mismo commit que quitó `go.sum` del índice, así que el árbol ya estaba arreglado cuando la puerta empezó a mirar | **sólo se ha visto fallar contra mutaciones de su propio autor** (M-A, M-B, M-C). La rama «con dependencias» no la recorre ninguna entrada real y no la va a recorrer hasta que entre la primera dependencia, que puede ser en etapas |
| `TestLaInstantaneaNoPublicaCardinalesQueElArbolYaDesmiente` | 04-09-2026 | **19-09-2026** | nació en el mismo commit que remidió la instantánea | **se estrenó en rojo sobre dato real, pero sobre el commit ANTERIOR** (`80627ed`), con tres contrastes a la vez. O sea que documenta un fallo que ya había ocurrido en vez de haberlo impedido. Es más de lo que puede decir la de arriba y sigue sin ser lo mismo que vigilar |
| `TestSinPublicadorLaPantallaNoOfreceLaPublicacion` | 06-09-2026 | **21-09-2026** | el cable (`Publica: est.PuedeGuardar && s.publicar != nil`) ya estaba puesto y era correcto: lo que no había era nadie recorriendo sus dos ramas | **no la pidió ninguna sospecha: la pidió el barrido de `godoc_vigilado_test.go`**, que encontró que el godoc de `Publicaciones` enunciaba el peligro y no nombraba a nadie. O sea que nace verde por construcción, y lo que sí ha demostrado ya es el valor del barrido que la encontró, que es otra cosa |
| `TestLasFormasDeRomperUnaPrueba` (el caso «prueba sobre una obligación que NO es observable») | 07-09-2026 | **22-09-2026** | el corpus no declaraba **ninguna** prueba cuando la puerta nació, así que no había nada que cazar: nació vigilando un bloque vacío, que es el verde más fácil que hay | **la única prueba real del corpus la escribí yo el mismo día**, y está sobre una obligación observable, así que la puerta sigue sin haber visto un caso que no le pusiera delante su autor. Lo que sí ha hecho ya es forzar una decisión al escribirla: `observable` vive en dos campos y contarlo por uno publicaba un techo cuatro veces más bajo. La revisión de dentro de quince días cae **después** de la campaña de la etapa 3, que es cuando entrarán pruebas escritas por otra pasada: ahí se sabrá si vigila |

| `TestLaLeyDeConservacionParaSiLosCubosNoSumanElTotal` | 07-09-2026 | **22-09-2026** | **ninguna entrada puede alcanzarla hoy**, y eso es peor que nacer verde: el contador del total y el parser del recolector manual recorren las mismas líneas, así que no hay fichero que las separe. Lo destapó la mutación M37, que sobrevivió | su prueba es **sintética a propósito** y lo dice el godoc: lo que vigila no es un fichero, es el parser del futuro, el día que alguien meta un `continue` de más en el bucle. La revisión de dentro de quince días no puede preguntar «¿ha cazado algo?», porque la respuesta va a ser no: lo que tiene que preguntar es si el bucle de `leer` ha cambiado, y si no ha cambiado, si sigue mereciendo la pena |

| `TestElCatalogoDiceDelEstadoLoMismoQueElNucleo` | 07-09-2026 | **22-09-2026** | nació en el **mismo commit** que escribió las once cadenas de `nucleo/estado` y sus dos traducciones, así que las tres mitades que compara las escribió la misma mano el mismo día. Es el verde de quien se examina de lo que acaba de estudiar | nació además con una comprobación de más: contrastaba los huecos `%s` del núcleo contra los del inglés, y **la mutación M5 demostró que era una tercera implementación** (`TestPuertaI18nElFormateoCasaEntreIdiomas` ya compara los verbos de toda clave contra el idioma por defecto, y la atadura letra por letra de aquí cierra el triángulo). Se quitó, con el motivo escrito en su sitio. Lo que queda es la atadura, y lo que de verdad va a decir si vigila es el día que alguien toque una redacción y se olvide de la otra, y ese día no ha llegado. Su hermana del acta (`TestElCatalogoDiceDelActaLoMismoQueElNucleo`) lleva desde el 01-09 y es la referencia con la que comparar en la revisión |
| `TestTodaRamaDeCalcularEmiteClaveYSusArgumentosCasan` | 07-09-2026 | **22-09-2026** | recorrió las once ramas de `Calcular` a la primera y las once estaban bien: el emparejamiento clave/argumentos lo construye `porque()` en un solo sitio, así que hoy **no hay manera de que se separen sin tocar esa función** | vigila un emparejamiento **posicional y sin firmar** (los `%s` se rellenan en orden), que es la clase que este repositorio lleva diez hallazgos persiguiendo, pero hoy tiene un solo constructor. **La pregunta de la revisión no es «¿ha cazado algo?» sino «¿ha aparecido un segundo sitio que construya un `Motivo`?»**: mientras `porque()` sea el único, esta puerta protege del futuro y no del presente, y eso hay que saberlo en vez de contarla como vigilancia |

| `TestCadaPrimitivaDelRelojDiceSiAlguienLaEnciende` | 08-09-2026 | **23-09-2026** | las seis primitivas implementadas están **las seis encendidas** hoy, así que su primera ejecución no podía encontrar nada. Nació de contestar una pregunta, no de una sospecha | **el caso que existe para cazar ya ocurrió y esta puerta llegó tarde**: `maximo` estuvo construido, con sus dorados en verde y **apagado para el corpus** durante semanas, con ocho retenciones del CRA esperándolo. O sea que documenta un fallo pasado en vez de haberlo impedido, igual que la de la instantánea. Lo que sí hizo el día que nació fue **desmentir una línea de `CLAUDE.md`**: decía que tres primitivas seguían apagadas y son dos, y las dos que quedan no existen ni como tipo, que no es lo mismo. La revisión tiene que preguntar si ha entrado alguna primitiva nueva: mientras no entre ninguna, esta puerta protege del futuro |
| `TestNingunPaquetePublicadoLlegaVacio` | 08-09-2026 | **23-09-2026** | se escribe en el **mismo commit** que deja los dos directorios como tienen que estar, así que su primer recorrido no puede encontrar nada. Es el verde por construcción | su control negativo recorre las dos direcciones con paquetes sintéticos, pero eso demuestra que el detector funciona, **no que haya algo que detectar**. Lo que va a decir si vigila es el día que alguien añada un esqueleto a `paquetes/` «para que cuente», que es exactamente como llegaron los doce. La revisión mira si `MinimoDeMarcos` y `EsqueletosEsperados` se han movido, y si se movieron juntos |
| `TestLaFraseDelIdiomaDelCorpusSoloSaleDondeEsCierta` | 08-09-2026 | **23-09-2026** | nace con la frase puesta en la única plantilla donde es cierta, así que no hay nada que prohibir | **no es del todo verde: se puso roja en su primera versión** y por eso la granularidad es la plantilla y no la superficie. Comparando superficies acusaba a `pantallas`, que pide la frase en `tabla.html` y pinta `Regla` en `hoy.html`: dos páginas de la misma superficie. Eso es más de lo que puede decir una puerta que nace verde del todo, y sigue sin ser lo mismo que vigilar. La revisión tiene que preguntar si alguien intentó reusar la clave en el calendario |

| `TestElMarcadorDiceDeCuandoSonSusNotas`, **sólo su rama del umbral** | 08-09-2026 | **23-09-2026** | **la puerta entera NO nació verde: nació roja** sobre `docs/marcador.md`, que no publicaba de cuándo eran sus notas. Lo que sí nace verde es su segunda mitad, la del umbral: la nota más vieja es del 04-09-2026 y hoy tiene **4 días** de los 14, así que la rama del descargo no la recorre ninguna entrada real | **esta fila tiene fecha de caducidad conocida y eso la hace rara**: si nadie vuelve a medir las notas, la rama se enciende sola el **18-09-2026**, un día antes de su revisión, y CI la pondrá roja con las dos salidas escritas en el mensaje. O sea que la revisión del 23-09 se hace SABIENDO ya si el aviso llegó y qué se hizo con él: volver a medir las diecisiete o escribir el descargo. **Y si alguien remidió antes del 18**, la rama sigue sin recorrerse y entonces la pregunta es la de siempre, si catorce días es el umbral correcto o si sólo es el que nunca se alcanza. Las dos ramas tienen control positivo con fecha sintética (`TestLasDosRamasDelUmbralDeFrescuraSeRecorren`), que demuestra que el detector funciona y no que haya algo que detectar |

## Cerradas

Ninguna todavía. La primera revisión toca el 19-09-2026.
